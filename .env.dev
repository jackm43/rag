# Secrets for the local debugging UI (`pnpm run dev:ui`). scripts/dev-ui.ts
# resolves the 1Password references in .env plus these (these win) with the
# 1Password SDK at launch; a variable already exported in the shell takes
# precedence over both.
CF_AIG_TOKEN="op://Services/ai gateway coding cloudflare token/password"
CLOUDFLARE_API_TOKEN="op://Services/Cloudflare User API Token/password"
